// BUILD:1783843000
// Configuración mínima de Next.js. Archivo nuevo (antes no existía).
// No cambia comportamiento existente; solo fija valores explícitos y seguros.
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
